// sentry.server.config.js — P13-4
// NOTE: Using @sentry/browser (client-side only).
// For server-side error capture, use @sentry/node in the Express backend (already configured).
// This file is intentionally minimal — no server-side Sentry for Next.js to avoid build conflicts.
